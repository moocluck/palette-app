import styled from 'styled-components';

const AuthorizationFormStyles = styled.section`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 24px;

    width: 100%;
`;

export default AuthorizationFormStyles;
