import { CSSProperties } from 'react';

export const cardStyles: CSSProperties = {
  backgroundColor: '#F5F3F7',
  borderRadius: '16px',
  padding: '48px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '32px',
  height: '100%',
};

export const linkStyles: CSSProperties = {
  textDecoration: 'none',
  width: '100%',
  height: '100%',
};

export const imageContainerStyles: CSSProperties = {
  width: '128px',
  height: '128px',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
};

export const imageStyles: CSSProperties = {
  maxWidth: '100%',
  maxHeight: '100%',
};

export const textStyles: CSSProperties = {
  textAlign: 'center',
};

